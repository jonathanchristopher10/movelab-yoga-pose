The branded photo frame shown on the Score screen — the thing a customer photographs with their own phone.

```jsx
<PhotoFrame photo={capturedDataUrl} poseName="Tree Pose" score={96} />
```

Mirrors the photo (selfie feel), overlays a bottom gradient, logo top-left, score badge top-right, pose name bottom-left in Anton.
