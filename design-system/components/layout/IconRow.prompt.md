A rule row for the "How to Play" screen: a hand-drawn icon inside a thin circle outline, plus a title.

```jsx
<IconRow icon="assets/icon-pose.png" title="Choose your pose" index={1} />
```

**Intentional addition** — the source README describes this row inline ("three stacked rules, each with an icon") without naming it as a component; it's broken out because it's a repeated pattern.
