The sage-green circular score ring on the Score screen. Animates from 0 to the final value with an eased count-up.

```jsx
<ProgressRing value={96} label="SCORE" />
```

Track is a light warm grey (`--score-track`), fill is `--accent`. Center shows the live percentage in Inter 800.
